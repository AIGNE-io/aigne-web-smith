# 管理更新歷史記錄

本指南將引導您稽核 WebSmith 所做的所有變更。`aigne web history` 命令會記錄每一次的產生或更新操作，讓您始終清楚知道變更了什麼、何時發生，以及為何套用該回饋。

本指南屬於更廣泛的 **更新網站** 工作流程。當您在發布前需要書面記錄、與團隊成員審查或還原編輯時，隨時可以執行此命令。

## 檢視歷史記錄日誌

使用以下命令顯示完整的更新日誌（最新的在最前面）：

```bash 檢視歷史記錄日誌 icon=lucide:history
aigne web history view
```

別名 `log` 和 `list` 會產生相同的輸出：

```bash 檢視歷史記錄日誌 (別名) icon=lucide:history
aigne web history log
```

如果尚無任何更新，WebSmith 會印出 `No update history found`。

## 瞭解每個項目

每一行都描述一個變更事件，並遵循相同的結構。

| 欄位 | 說明 |
| :--- | :--- |
| **簡短雜湊值** | 從時間戳衍生出的確定性識別碼。與您的團隊討論特定更新時可使用它。 |
| **日期** | 相對時間戳（例如 `5 minutes ago` 或 `2 days ago`）。超過一週的項目會顯示日曆日期。 |
| **操作** | `structure_update` 用於網站地圖變更，`page_update` 用於個別頁面編輯。 |
| **頁面路徑** | 僅針對 `page_update` 項目顯示，並出現在括號中（例如 `(about-us)`）。 |
| **回饋** | 您在執行 `aigne web update` 時提供的摘要，記錄下來以便未來的讀者理解其意圖。 |

### 範例輸出

```text
📜 Update History

e5c7a91 5 minutes ago   page_update (about-us): Refined the company mission statement
a3b4f8e 2 hours ago     page_update (services): Added new section for consulting services
1d9c0b2 1 day ago       structure_update: Added a new "Careers" page to the main menu
f4e5a67 3 days ago      page_update (contact): Updated the main office phone number
```

在發布前使用歷史記錄日誌來確認哪些編輯即將上線，或在發布後用來回答利害關係人關於某項內容何時變更的問題。

## 相關工作流程

- [更新結構](./guides-update-website-update-structure.md) – 重新產生導覽和網站地圖。
- [更新頁面](./guides-update-website-update-page.md) – 修改個別頁面上的文案。