# 管理更新历史

本指南将指导您审计 WebSmith 所做的所有更改。`aigne web history` 命令会记录每一次生成或更新操作，因此您始终可以了解更改了什么、何时发生以及为何应用了反馈。

本指南属于更广泛的 **更新网站** 工作流程。在发布、与团队成员审查或回滚编辑之前，当您需要一份书面记录时，可以随时运行它。

## 查看历史日志

使用以下命令显示完整的更新日志（最近的记录排在最前面）：

```bash 查看历史日志 icon=lucide:history
aigne web history view
```

别名 `log` 和 `list` 会产生相同的输出：

```bash 查看历史日志（别名） icon=lucide:history
aigne web history log
```

如果尚无更新，WebSmith 会打印 `No update history found`。

## 理解每个条目

每一行都描述一个变更事件，并遵循相同的结构。

| 字段 | 说明 |
| :--- | :--- |
| **短哈希值** | 源自时间戳的确定性标识符。与团队讨论特定更新时可使用它。 |
| **日期** | 相对时间戳（例如，`5 minutes ago` 或 `2 days ago`）。超过一周的条目会显示日历日期。 |
| **操作** | `structure_update` 表示站点地图更改，`page_update` 表示单个页面编辑。 |
| **页面路径** | 仅对 `page_update` 条目显示，并出现在括号中（例如 `(about-us)`）。 |
| **反馈** | 您在运行 `aigne web update` 时提供的摘要，记录下来以便未来的读者理解其意图。 |

### 示例输出

```text
📜 Update History

e5c7a91 5 minutes ago   page_update (about-us): Refined the company mission statement
a3b4f8e 2 hours ago     page_update (services): Added new section for consulting services
1d9c0b2 1 day ago       structure_update: Added a new "Careers" page to the main menu
f4e5a67 3 days ago      page_update (contact): Updated the main office phone number
```

在发布前使用历史日志来确认哪些编辑即将上线，或在发布后用于回答利益相关者关于某项内容何时更改的问题。

## 相关工作流程

- [更新结构](./guides-update-website-update-structure.md) – 重新生成导航和站点地图。
- [更新页面](./guides-update-website-update-page.md) – 优化单个页面上的文案。