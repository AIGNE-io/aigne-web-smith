type: image
name: generateImage
instructions: |
  {{aiPrompt}} , high resolution, web design friendly, professional, clear, relevant to AI/tech industry, clean composition, vibrant colors but not distracting, soft lighting, modern aesthetic, suitable for a product landing page. Avoid busy backgrounds, ensure main subject is prominent.
input_schema:
  type: object
  properties:
    aiPrompt:
      type: string
      description: The AI prompt to generate the image.
  required:
    - aiPrompt
