type: team
name: generateCoverTeam
description: Generate cover image for the website
task_title: Generate cover image for the website
task_render_mode: collapse
skills:
  - ../image/generate-image.yaml
  - ./save-cover.mjs
input_schema:
  type: object
  properties:
    aiPrompt:
      type: string
      description: The AI prompt to generate the cover image.
  required:
    - aiPrompt
output_schema:
  type: object
  properties:
    message:
      type: string
      description: A message describing the result of the cover image generation.