name: middleFormatToStructuredData
description: 将中间格式YAML转换为结构化的组件数据数组
instructions:
  url: ../../prompts/pages-format/middle-format-to-structured-data.md
input_schema:
  type: object
  properties:
    middleFormatContent:
      type: string
      description: 中间格式YAML内容
    feedback:
      type: string
      description: 上一轮验证的反馈信息
      default: ""
  required:
    - middleFormatContent
output_schema:
  type: object
  properties:
    structuredData:
      type: string
      description: 结构化的组件数据JSON数组
  required:
    - structuredData
