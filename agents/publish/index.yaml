type: team
name: publish
alias:
  - pub
  - p
description: Publish the website to Pages Kit
skills:
  - url: ../init/index.mjs
    default_input:
      skipIfExists: true
  - ../utils/load-config.mjs
  - ./publish-website.mjs
input_schema:
  type: object
  properties:
    appUrl:
      type: string
      description: target website URL where the pages will be published (optional - if not provided, will prompt for interactive input)
