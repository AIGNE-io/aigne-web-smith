type: team
name: publish
alias:
  - pub
  - p
description: Publish the pages to Pages Kit
skills:
  - url: ./input-generator.mjs
    default_input:
      skipIfExists: true
  - load-config.mjs
  - pages-publish.mjs
input_schema:
  type: object
  properties:
    appUrl:
      type: string
      description: target website URL where the pages will be published (optional - if not provided, will prompt for interactive input)
