type: team
name: generateTheme
description: Create and save a comprehensive theme configuration
skills:
  - ../utils/load-config.mjs
  - ./collect-theme-suggestion.mjs
  - ./generate-theme.yaml
  - ./save-theme.mjs
input_schema:
  type: object
  properties:
    name:
      type: string
      description: Name for your theme
    config:
      type: string
      description: Configuration file location
    suggestion:
      type: string
      description: User suggestions for theme generation
mode: sequential
taskTitle: Generate theme based on website configuration and user suggestions
task_render_mode: collapse
